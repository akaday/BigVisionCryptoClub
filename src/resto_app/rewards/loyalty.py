def earn_loyalty_points(user_id, points):
    print(f"{points} points earned for user {user_id}.")

def redeem_loyalty_points(user_id, points):
    print(f"{points} points redeemed for user {user_id}.")
